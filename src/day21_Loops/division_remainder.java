package day21_Loops;

import com.sun.xml.internal.ws.api.model.wsdl.WSDLOutput;

import java.util.Scanner;

public class division_remainder {
    public static void main(String[] args) {
        /*
       3. write a program that can divide two
        numbers without using / or % operators and
         returns the division and remainder
			ex:
				input:	10
						3
	output: 3 with a remainder of 1
         */

     /*  Scanner scan = new Scanner(System.in);
​       System.out.println("Enter first number: ");
       int num1 = scan.nextInt();
       System.out.println("Enter second number: ");
       int num2 = scan.nextInt();
           scan.nextLine();
       if (num2 == 0){
           System.out.println("Invalid input!");
       }
        int count = 0;
       while(num1 >= num2){
           num1 -= num2;
           count++;
       }
        System.out.println(count+" with a remainder of "+ num1);

        System.out.println("==================");
       int a = 20;
        int b = 3;

        if (b ==0){
            System.out.println("Invalid input");
            System.exit(0);
        }
​        int count1 = 0;

        while(a >= b){
            a -= b;
        count1++;
        }
        System.out.println(count1+" with a remainder of "+ a);

    }
*/
    }
}